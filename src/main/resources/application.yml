spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marciotenorio/distprog-config-server-repo
      discovery:
        enabled: true
      label: master

server:
  port: 8888

#spring.cloud.config.server.git.search-paths={application}-{profile}

